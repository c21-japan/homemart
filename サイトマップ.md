# 🗺️ サイトマップ

> **重要**: 新しい機能を開発する前に、必ずこのサイトマップを確認してください。
> システムの全体像を理解し、既存の構造やパターンに沿った開発を行うことで、
> 一貫性のある保守しやすいコードベースを維持できます。

**最終更新**: 2026-02-07
**バージョン**: 1.0.0

---

## 📋 目次

1. [プロジェクト概要](#プロジェクト概要)
2. [システム全体図](#システム全体図)
3. [技術スタック](#技術スタック)
4. [ディレクトリ構造](#ディレクトリ構造)
5. [主要機能マップ](#主要機能マップ)
6. [データベース設計](#データベース設計)
7. [API設計](#api設計)
8. [認証・権限システム](#認証権限システム)
9. [外部連携システム](#外部連携システム)
10. [開発ルール](#開発ルール)
11. [新機能開発フロー](#新機能開発フロー)

---

## 🎯 プロジェクト概要

**プロジェクト名**: HomeMart（不動産・リフォーム統合業務管理システム）

**目的**: 不動産売買・リフォーム業務における顧客管理、物件管理、勤怠管理、
マーケティング、会計連携を一元管理するエンタープライズシステム

**主要利用者**:
- 経営者・管理者（OWNER）
- 営業スタッフ（STAFF）
- アルバイトスタッフ（PART_TIME）
- 顧客（一般ユーザー）

---

## 🌐 システム全体図

```
┌─────────────────────────────────────────────────────────────┐
│                      フロントエンド層                          │
│  ┌──────────────────┐  ┌──────────────────┐                │
│  │   公開ページ      │  │   管理画面        │                │
│  │  - 売却フロー     │  │  - 顧客管理       │                │
│  │  - 購入フロー     │  │  - リード管理     │                │
│  │  - リフォーム     │  │  - 物件管理       │                │
│  │  - 物件一覧       │  │  - 勤怠管理       │                │
│  │  - リード登録     │  │  - チラシ管理     │                │
│  │  - SUUMO掲載      │  │  - 工程表         │                │
│  └──────────────────┘  │  - 相続DM         │                │
│   Next.js 15 + React   │  - 会計レポート   │                │
│   TypeScript + Tailwind│  - 会計レポート   │                │
│                        │  - ユーザー管理   │                │
│                        └──────────────────┘                │
└─────────────────────────────────────────────────────────────┘
                              ↓↑
┌─────────────────────────────────────────────────────────────┐
│                      認証・権限層                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐         │
│  │  Clerk   │  │   JWT    │  │  権限チェック      │         │
│  │  認証    │→ │  セッション│→ │ Feature-Based    │         │
│  └──────────┘  └──────────┘  │  RBAC            │         │
│                              └──────────────────┘         │
└─────────────────────────────────────────────────────────────┘
                              ↓↑
┌─────────────────────────────────────────────────────────────┐
│                      API・ビジネスロジック層                   │
│  ┌────────────────────────────────────────────────────┐     │
│  │  Next.js API Routes + Server Actions              │     │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │     │
│  │  │顧客管理  │ │物件管理  │ │外部API統合       │  │     │
│  │  │API       │ │API       │ │(Google/freee/LINE)│  │     │
│  │  └──────────┘ └──────────┘ └──────────────────┘  │     │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │     │
│  │  │リード管理│ │勤怠管理  │ │画像処理          │  │     │
│  │  │API       │ │API       │ │(Cloudinary)      │  │     │
│  │  └──────────┘ └──────────┘ └──────────────────┘  │     │
│  └────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
                              ↓↑
┌─────────────────────────────────────────────────────────────┐
│                      データベース層                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Supabase (PostgreSQL 15)                           │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │   │
│  │  │顧客DB   │ │物件DB   │ │勤怠DB   │ │権限DB   │   │   │
│  │  │(12テーブル)│(5テーブル)│(7テーブル)│(6テーブル)│   │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘   │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐              │   │
│  │  │リードDB │ │チラシDB │ │外部連携 │              │   │
│  │  │(5テーブル)│(8テーブル)│(3テーブル)│              │   │
│  │  └─────────┘ └─────────┘ └─────────┘              │   │
│  │  + RLS (Row Level Security) で行レベルセキュリティ   │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↓↑
┌─────────────────────────────────────────────────────────────┐
│                      外部サービス層                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │  Google  │ │  freee   │ │   LINE   │ │Cloudinary│      │
│  │  Calendar│ │  会計    │ │ Messaging│ │  画像CDN │      │
│  │  Sheets  │ │  API     │ │   API    │ │          │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 技術スタック

### フロントエンド
| カテゴリ | 技術 | バージョン | 用途 |
|---------|------|-----------|------|
| フレームワーク | Next.js | 15.5.10 | App Router、SSR、API Routes |
| UI ライブラリ | React | 19.2.1 | コンポーネントベースUI |
| 言語 | TypeScript | 5.x | 型安全性 |
| スタイリング | Tailwind CSS | 3.4.17 | ユーティリティファーストCSS |
| UI コンポーネント | Headless UI | 2.2.7 | アクセシビリティ対応UI |
| フォーム管理 | React Hook Form | 7.62.0 | フォーム状態管理 |
| バリデーション | Zod | 4.0.17 | スキーマ検証 |
| 日付処理 | date-fns | 4.1.0 | 日付フォーマット・計算 |
| PDF生成 | @react-pdf/renderer | 4.3.0 | PDF帳票出力 |

### バックエンド
| カテゴリ | 技術 | バージョン | 用途 |
|---------|------|-----------|------|
| ランタイム | Node.js | - | サーバーサイド実行環境 |
| データベース | Supabase | - | PostgreSQL + リアルタイム機能 |
| ORM | Prisma | 6.14.0 | データベースアクセス |
| 認証 | Clerk | 6.31.1 | ユーザー認証 |
| セッション | JWT (jose) | 6.1.3 | トークンベース認証 |
| CSV処理 | csv-parse | 6.1.0 | CSV解析 |

### 外部連携
| サービス | SDK/ライブラリ | 用途 |
|---------|--------------|------|
| Google APIs | googleapis 155.0.1 | カレンダー・Sheets連携 |
| freee | - | 会計・請求管理 |
| LINE | - | LINEメッセージング |
| Cloudinary | next-cloudinary 6.16.0 | 画像アップロード・加工 |
| Mailjet | node-mailjet 6.0.9 | メール送信 |
| Sentry | @sentry/nextjs 10.5.0 | エラー監視 |

### 開発ツール
| ツール | バージョン | 用途 |
|-------|-----------|------|
| ESLint | 9.x | コード品質チェック |
| Playwright | 1.54.2 | E2Eテスト |
| Husky | - | Git Hooks |

---

## 📁 ディレクトリ構造

```
homemart/
├── app/                              # Next.js App Router
│   ├── admin/                        # 🔐 管理画面（30モジュール）
│   │   ├── customers/                # 顧客管理CRUD
│   │   ├── leads/                    # リード管理CRUD
│   │   ├── properties/               # 物件管理CRUD
│   │   ├── attendance/               # 勤怠管理
│   │   ├── part-time-attendance/     # アルバイト勤怠
│   │   ├── flyers/                   # チラシマーケティング
│   │   ├── reform-projects/          # 施工実績管理
│   │   ├── construction-schedule/    # 工程表管理
│   │   ├── inheritance-dm/           # 相続DM（準備中）
│   │   ├── internal-applications/    # 社内申請
│   │   ├── users/                    # ユーザー管理
│   │   ├── accounting/               # 会計レポート
│   │   └── ...その他18モジュール
│   │
│   ├── api/                          # 🌐 REST API エンドポイント
│   │   ├── auth/                     # 認証API
│   │   ├── customers/                # 顧客API
│   │   ├── leads/                    # リードAPI
│   │   ├── properties/               # 物件API
│   │   ├── freee/                    # freee連携API
│   │   ├── google/                   # Google API連携
│   │   ├── integrations/             # 外部連携（LINE等）
│   │   ├── photos/                   # 画像処理API
│   │   ├── cron/                     # 定期実行タスク
│   │   └── ...その他
│   │
│   ├── (secure)/                     # 🔒 認証必須ルート
│   ├── sell/                         # 🏡 売却フロー（公開）
│   ├── buy/                          # 🏠 購入フロー（公開）
│   ├── reform/                       # 🔨 リフォームフロー（公開）
│   ├── properties/                   # 📍 物件一覧（公開）
│   ├── properties-new/[id]/          # 🏠 新築戸建 詳細（公開）
│   ├── properties-new/               # 🏠 新築戸建 一覧（公開）
│   ├── properties-used/              # 🏠 中古戸建 一覧（公開）
│   ├── properties-land/              # 🏠 土地 一覧（公開）
│   ├── properties-mansion/           # 🏠 マンション 一覧（公開）
│   └── ...その他
│
├── components/                       # ⚛️ React コンポーネント
│   ├── admin/                        # 管理画面専用コンポーネント
│   ├── customers/                    # 顧客関連コンポーネント
│   ├── flyers/                       # チラシ関連コンポーネント
│   ├── ui/                           # 共通UIコンポーネント
│   └── PublicShell.tsx               # 公開ページのヘッダー/フッター包み（/adminは除外）
│
├── lib/                              # 📚 ビジネスロジック・ユーティリティ
│   ├── auth/                         # 🔐 認証・権限管理
│   │   ├── permissions.ts            # 権限チェック関数
│   │   ├── session.ts                # セッション管理
│   │   └── auth-service.ts           # 認証サービス
│   ├── supabase/                     # 🗄️ Supabaseユーティリティ
│   │   ├── server.ts                 # サーバーサイドクライアント
│   │   ├── customers.ts              # 顧客DB操作
│   │   ├── leads.ts                  # リードDB操作
│   │   ├── properties.ts             # 物件DB操作
│   │   └── ...その他
│   ├── freee/                        # 💰 freee会計連携
│   │   ├── client.ts                 # freee APIクライアント
│   │   ├── reports.ts                # レポート処理
│   │   └── csv-parser.ts             # CSV解析
│   ├── middleware/                   # 🛡️ ミドルウェア
│   └── utils/                        # 🔧 ユーティリティ関数
│
├── server/
│   └── actions/                      # 🎬 Next.js Server Actions
│       └── customers.ts              # 顧客操作アクション
│
├── types/                            # 📝 TypeScript型定義
│   ├── database.ts                   # DB型定義
│   ├── leads.ts                      # リード型定義
│   ├── flyers.ts                     # チラシ型定義
│   └── supabase.ts                   # Supabase型定義
│
├── hooks/                            # 🪝 React Custom Hooks
│   ├── useGoogleAuth.ts              # Google認証フック
│   ├── useRecentlyViewed.ts          # 閲覧履歴フック
│   └── ...
│
├── config/                           # ⚙️ 設定ファイル
│   ├── permissions.json              # 権限設定マスタ（19機能×6権限）
│   ├── users.json                    # ユーザー設定
│   └── production.ts                 # 本番環境設定
│
├── supabase/                         # 🗃️ Supabase設定
│   ├── migrations/                   # DBマイグレーション
│   └── config.toml                   # Supabase設定
│
├── public/                           # 🌐 静的アセット
│   └── suumo/                        # SUUMOクローリング結果（properties.json + images/）
│
├── scripts/                          # 🚀 セットアップ・シードスクリプト
│   ├── setup-supabase.js             # Supabase初期化
│   ├── crawl-suumo.mjs               # SUUMOクローラ（取得 + ロゴ合成）
│   └── seed-customers.ts             # 顧客データシード
│
├── tests/                            # 🧪 E2Eテスト（Playwright）
├── database-*.sql                    # 📊 DB初期化スクリプト（20ファイル）
└── deploy-*.sql                      # 🚢 デプロイ用スクリプト
```

### ディレクトリの役割と責務

| ディレクトリ | 役割 | 注意点 |
|------------|------|--------|
| `app/admin/` | 管理画面のページコンポーネント | 必ず認証・権限チェックを含める |
| `app/api/` | API Route Handler | エラーハンドリングと権限チェック必須 |
| `components/` | 再利用可能なUIコンポーネント | ビジネスロジックを含めない |
| `lib/` | ビジネスロジック・DB操作 | **最も重要**。ロジックはここに集約 |
| `types/` | 型定義 | DBスキーマと同期させる |
| `config/` | 設定ファイル | 環境変数は使わず、ファイルで管理 |

---

## 🎯 主要機能マップ

### コア業務機能（9モジュール）

```
┌─────────────────────────────────────────────────────────────┐
│                      顧客管理システム                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  顧客データ → リード管理 → 商談 → 成約 → フォロー   │   │
│  │     ↓           ↓          ↓       ↓        ↓        │   │
│  │  customers  customer_leads  meeting  contracts  after │   │
│  └──────────────────────────────────────────────────────┘   │
│  実装場所: app/admin/customers/, lib/supabase/customers.ts  │
│  API: /api/customers/*                                      │
│  権限: customers.VIEW, customers.CREATE, customers.EDIT     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      物件管理システム                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  物件登録 → 写真管理 → 売主リンク → 公開/非公開     │   │
│  │     ↓          ↓           ↓            ↓            │   │
│  │  properties  lead_photos  properties_  status        │   │
│  │                          seller_link                  │   │
│  └──────────────────────────────────────────────────────┘   │
│  実装場所: app/admin/properties/, lib/supabase/properties.ts│
│  API: /api/properties/*                                     │
│  権限: properties.VIEW, properties.CREATE, properties.EDIT  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      リード管理システム                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  リード獲得 → ステータス管理 → 顧客変換              │   │
│  │     ↓              ↓               ↓                 │   │
│  │  customer_leads  lead_status  customer_conversion   │   │
│  └──────────────────────────────────────────────────────┘   │
│  実装場所: app/admin/leads/, lib/supabase/leads.ts          │
│  API: /api/leads/*                                          │
│  権限: leads.VIEW, leads.CREATE, leads.EDIT                 │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      勤怠管理システム                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  出社打刻 → 勤務時間 → 退社打刻 → 月次集計          │   │
│  │     ↓          ↓          ↓           ↓              │   │
│  │  punch_in() attendance punch_out()  monthly_view    │   │
│  └──────────────────────────────────────────────────────┘   │
│  実装場所: app/admin/attendance/, app/admin/part-time-attendance/ │
│  API: /api/part-time-attendance/realtime                    │
│  権限: attendance.VIEW, attendance.APPROVE                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   チラシマーケティングシステム                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  デザイン作成 → AI生成 → 配布 → 反響測定            │   │
│  │       ↓          ↓        ↓        ↓                 │   │
│  │  flyer_designs  AI API  distributions  inquiries    │   │
│  └──────────────────────────────────────────────────────┘   │
│  実装場所: app/admin/flyers/, components/flyers/             │
│  API: /api/flyers/*, /api/flyers/ai-generate-design        │
│  権限: flyers.VIEW, flyers.CREATE, flyers.EDIT              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      施工実績管理                              │
│  実装場所: app/admin/reform-projects/                         │
│  権限: reform_projects.VIEW, reform_projects.EDIT           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      工程表管理                                │
│  実装場所: app/admin/construction-schedule/                   │
│  権限: construction_schedule.VIEW, construction_schedule.EDIT│
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      社内申請システム                          │
│  実装場所: app/admin/internal-applications/                   │
│  権限: internal_applications.VIEW, internal_applications.CREATE│
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      ユーザー管理                              │
│  実装場所: app/admin/users/, lib/auth/                        │
│  API: /api/admin/users/*                                     │
│  権限: users.VIEW, users.CREATE, users.EDIT                  │
└─────────────────────────────────────────────────────────────┘
```

### 公開ページ（顧客向け）

```
┌────────────┐   ┌────────────┐   ┌────────────┐
│  売却フロー │   │  購入フロー │   │リフォーム  │
│  /sell     │   │  /buy      │   │  /reform   │
│            │   │            │   │            │
│ 媒介契約   │   │ 予算設定   │   │ 見積依頼   │
│ レインズ   │   │ 物件検索   │   │ 施工管理   │
│ 定期報告   │   │ 住宅ローン │   │ 完工報告   │
└────────────┘   └────────────┘   └────────────┘
```

---

## 🗄️ データベース設計

### テーブル一覧（62テーブル）

#### 顧客関連（12テーブル）
```
customers                    # 顧客マスタ
customer_leads               # 見込客情報
seller_details               # 売却者詳細
buyer_details                # 購入者詳細
reform_projects              # リフォーム案件
meeting_records              # 打ち合わせ記録
checklist_items              # チェックリスト
activity_logs                # アクティビティログ
lead_attachments             # 添付ファイル
```

#### 物件関連（5テーブル）
```
properties                   # 物件マスタ
properties_seller_link       # 物件-売却者関連付け
lead_photos                  # 物件写真
related_properties           # 関連物件
```

#### 勤怠関連（7テーブル）
```
employees                    # 従業員マスタ
attendance_records           # 勤怠打刻記録
shift_requests               # シフト申請
part_time_attendance         # アルバイト勤怠
attendance_daily_view        # 日次勤務時間ビュー
attendance_monthly_view      # 月次勤務時間ビュー
```

#### チラシ関連（8テーブル）
```
flyer_designs                # チラシデザイン
flyer_distributions          # 配布情報
flyer_inquiries              # チラシ経由問い合わせ
flyer_prints                 # 印刷費用
```

#### 権限関連（6テーブル）
```
auth_users                   # ユーザー認証
user_profiles                # ユーザープロフィール
permissions                  # 権限マスタ
role_permissions             # ロール別権限
user_custom_permissions      # ユーザー個別権限
user_sessions                # セッション管理
```

#### 外部連携（3テーブル）
```
freee_reports                # freee連携レポート
listing_agreements           # 媒介契約
listing_report_logs          # 定期報告ログ
```

### データベースER図（簡略版）

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│  customers  │◄────────│customer_leads│────────►│ properties  │
│  (顧客)     │ 1     n │  (リード)    │ n     1 │  (物件)     │
└─────────────┘         └─────────────┘         └─────────────┘
       │                       │                        │
       │ 1                     │ 1                      │ 1
       │                       │                        │
       │ n                     │ n                      │ n
       ▼                       ▼                        ▼
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│meeting_records│         │lead_attachments│         │ lead_photos │
│(打ち合わせ)  │         │  (添付)      │         │  (写真)     │
└─────────────┘         └─────────────┘         └─────────────┘

┌─────────────┐         ┌─────────────┐
│  employees  │◄────────│attendance_records│
│  (従業員)   │ 1     n │  (勤怠)     │
└─────────────┘         └─────────────┘

┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│  auth_users │◄────────│user_profiles │────────►│role_permissions│
│  (認証)     │ 1     1 │ (プロフィール)│ n     n │  (権限)     │
└─────────────┘         └─────────────┘         └─────────────┘
```

### 主要テーブルスキーマ

#### customers（顧客マスタ）
```sql
CREATE TABLE customers (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    customer_id VARCHAR(50) UNIQUE,        -- 顧客番号（例: C-2024-0001）
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255),
    phone VARCHAR(50),
    address TEXT,
    status VARCHAR(50),                    -- active, inactive, prospect
    source VARCHAR(100),                   -- 流入経路
    assigned_to UUID,                      -- 担当者（auth_users.id）
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

#### customer_leads（リード）
```sql
CREATE TABLE customer_leads (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    lead_number VARCHAR(50) UNIQUE,        -- リード番号（例: L-2024-0001）
    customer_id UUID REFERENCES customers(id),
    property_id UUID REFERENCES properties(id),
    lead_type VARCHAR(50),                 -- sell, buy, reform
    status VARCHAR(50),                    -- new, contacted, qualified, lost, converted
    priority VARCHAR(20),                  -- high, medium, low
    estimated_value DECIMAL(15,2),
    notes TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

#### properties（物件）
```sql
CREATE TABLE properties (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    property_id VARCHAR(50) UNIQUE,        -- 物件番号（例: P-2024-0001）
    title VARCHAR(255) NOT NULL,
    address TEXT,
    property_type VARCHAR(50),             -- house, apartment, land
    price DECIMAL(15,2),
    area DECIMAL(10,2),
    rooms INT,
    status VARCHAR(50),                    -- available, sold, under_contract
    description TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🌐 API設計

### APIエンドポイント一覧（40+ API）

#### 認証API
```
POST   /api/auth/login                    # ログイン
GET    /api/auth/me                       # 現在ユーザー情報取得
POST   /api/auth/logout                   # ログアウト
```

#### 顧客管理API
```
GET    /api/customers                     # 顧客一覧取得
GET    /api/customers/search              # 顧客検索
POST   /api/customers                     # 顧客作成
GET    /api/customers/[id]                # 顧客詳細取得
PUT    /api/customers/[id]                # 顧客更新
DELETE /api/customers/[id]                # 顧客削除
```

#### リード管理API
```
GET    /api/leads                         # リード一覧取得
POST   /api/leads                         # リード作成
GET    /api/leads/[id]                    # リード詳細取得
PUT    /api/leads/[id]                    # リード更新
DELETE /api/leads/[id]                    # リード削除
GET    /api/leads/[id]/fp-info            # FP情報取得
```

#### 物件管理API
```
GET    /api/properties                    # 物件一覧取得
POST   /api/properties                    # 物件作成
GET    /api/properties/[id]               # 物件詳細取得
PUT    /api/properties/[id]               # 物件更新
DELETE /api/properties/[id]               # 物件削除
```

#### 管理者API
```
GET    /api/admin/users                   # ユーザー一覧取得
POST   /api/admin/users                   # ユーザー作成
GET    /api/admin/users/[id]              # ユーザー詳細取得
PUT    /api/admin/users/[id]              # ユーザー更新
PUT    /api/admin/users/[id]/permissions  # 権限更新
GET    /api/admin/stats                   # 統計情報取得
```

#### 外部連携API
```
POST   /api/google/auth                   # Google認証
POST   /api/google/auth/refresh           # トークンリフレッシュ
POST   /api/google/data                   # Google Sheetsデータ同期
GET    /api/freee/reports                 # freeeレポート取得
POST   /api/freee/csv/upload              # CSVアップロード
GET    /api/freee/csv/data                # CSVデータ取得
POST   /api/integrations/line             # LINE Webhook
POST   /api/integrations/century21        # Century21連携
```

#### 画像・ファイルAPI
```
POST   /api/photos/presign                # 画像アップロード署名
POST   /api/photos/process                # 画像処理
POST   /api/upload-image                  # 画像アップロード
```

### API実装パターン

#### API Route Handler（Next.js 15）
```typescript
// app/api/customers/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { getAuth } from '@/lib/auth/session';
import { checkPermission } from '@/lib/auth/permissions';

export async function GET(request: NextRequest) {
  // 1. 認証チェック
  const user = await getAuth(request);
  if (!user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  // 2. 権限チェック
  const hasPermission = await checkPermission(user.id, 'customers', 'VIEW');
  if (!hasPermission) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 });
  }

  // 3. ビジネスロジック（lib/から呼び出し）
  const customers = await getCustomers();

  // 4. レスポンス返却
  return NextResponse.json({ data: customers });
}
```

#### Server Actions（Next.js 15）
```typescript
// server/actions/customers.ts
'use server';

import { revalidatePath } from 'next/cache';
import { createCustomer } from '@/lib/supabase/customers';

export async function createCustomerAction(formData: FormData) {
  // 1. バリデーション
  const validated = customerSchema.parse(formData);

  // 2. DB操作
  const customer = await createCustomer(validated);

  // 3. キャッシュ再検証
  revalidatePath('/admin/customers');

  return { success: true, data: customer };
}
```

---

## 🔐 認証・権限システム

### 認証フロー

```
┌─────────────────────────────────────────────────────────────┐
│                      認証フロー                                │
│                                                               │
│  1. ユーザーログイン                                           │
│     │                                                         │
│     ▼                                                         │
│  2. Clerk認証                                                 │
│     │                                                         │
│     ▼                                                         │
│  3. JWTトークン発行                                           │
│     │                                                         │
│     ▼                                                         │
│  4. セッション作成（user_sessions）                            │
│     │                                                         │
│     ▼                                                         │
│  5. ユーザー情報取得（auth_users）                             │
│     │                                                         │
│     ▼                                                         │
│  6. 権限情報取得（role_permissions + user_custom_permissions） │
│     │                                                         │
│     ▼                                                         │
│  7. アクセス許可                                               │
└─────────────────────────────────────────────────────────────┘
```

### 権限システム（Feature-Based RBAC）

#### 権限設定ファイル（config/permissions.json）

```json
{
  "features": {
    "customers": {
      "name": "顧客管理",
      "description": "顧客情報の管理",
      "permissions": ["VIEW", "CREATE", "EDIT", "DELETE", "EXPORT"]
    },
    "leads": {
      "name": "リード管理",
      "description": "見込客の管理",
      "permissions": ["VIEW", "CREATE", "EDIT", "DELETE"]
    },
    "properties": {
      "name": "物件管理",
      "description": "不動産物件の管理",
      "permissions": ["VIEW", "CREATE", "EDIT", "DELETE"]
    },
    "attendance": {
      "name": "勤怠管理",
      "description": "従業員の勤怠管理",
      "permissions": ["VIEW", "APPROVE", "EXPORT"]
    },
    "flyers": {
      "name": "チラシ管理",
      "description": "マーケティングチラシの管理",
      "permissions": ["VIEW", "CREATE", "EDIT", "DELETE"]
    }
    // ... 他14機能
  },
  "roles": {
    "OWNER": {
      "name": "所有者",
      "permissions": "*"  // 全権限
    },
    "ADMIN": {
      "name": "管理者",
      "permissions": [
        "customers.*",
        "leads.*",
        "properties.*",
        "attendance.VIEW",
        "attendance.APPROVE"
      ]
    },
    "STAFF": {
      "name": "スタッフ",
      "permissions": [
        "customers.VIEW",
        "leads.VIEW",
        "properties.VIEW"
      ]
    },
    "PART_TIME": {
      "name": "アルバイト",
      "permissions": [
        "attendance.VIEW"
      ]
    }
  }
}
```

#### 権限チェック関数（lib/auth/permissions.ts）

```typescript
/**
 * ユーザーが特定の機能に対する権限を持っているかチェック
 * @param userId - ユーザーID
 * @param feature - 機能名（例: 'customers', 'leads'）
 * @param permission - 権限タイプ（例: 'VIEW', 'CREATE', 'EDIT'）
 * @returns boolean
 */
export async function checkPermission(
  userId: string,
  feature: string,
  permission: PermissionType
): Promise<boolean> {
  // 1. ユーザーのロールを取得
  const userRole = await getUserRole(userId);

  // 2. ロール別権限をチェック
  const rolePermissions = await getRolePermissions(userRole);
  if (rolePermissions.includes(`${feature}.${permission}`) || rolePermissions.includes('*')) {
    return true;
  }

  // 3. ユーザー個別権限をチェック
  const customPermissions = await getUserCustomPermissions(userId);
  if (customPermissions.includes(`${feature}.${permission}`)) {
    return true;
  }

  return false;
}
```

### Row Level Security (RLS)

データベースレベルでのセキュリティポリシー:

```sql
-- 顧客データは担当者と管理者のみ閲覧可能
CREATE POLICY "customers_select_policy" ON customers
  FOR SELECT
  USING (
    auth.uid() = assigned_to OR
    EXISTS (
      SELECT 1 FROM user_profiles
      WHERE id = auth.uid() AND role IN ('OWNER', 'ADMIN')
    )
  );
```

---

## 🔗 外部連携システム

### Google API連携

```
┌─────────────────────────────────────────────────────────────┐
│                    Google API連携フロー                        │
│                                                               │
│  1. OAuth2認証                                                │
│     ├─ POST /api/google/auth                                 │
│     └─ リフレッシュトークン保存                               │
│                                                               │
│  2. Googleカレンダー連携                                       │
│     ├─ 打ち合わせ予定の自動同期                               │
│     └─ リマインダー通知                                       │
│                                                               │
│  3. Google Sheets連携                                         │
│     ├─ POST /api/google/data                                 │
│     ├─ 顧客データのインポート                                 │
│     └─ レポートのエクスポート                                 │
└─────────────────────────────────────────────────────────────┘
```

実装場所: `lib/google/`, `app/api/google/`, `hooks/useGoogleAuth.ts`

### freee会計連携

```
┌─────────────────────────────────────────────────────────────┐
│                    freee会計連携フロー                         │
│                                                               │
│  1. CSV データアップロード                                     │
│     ├─ POST /api/freee/csv/upload                            │
│     └─ Supabaseに保存                                         │
│                                                               │
│  2. データ解析                                                 │
│     ├─ lib/freee/csv-parser.ts                               │
│     └─ 売上・経費の自動分類                                   │
│                                                               │
│  3. レポート生成                                               │
│     ├─ GET /api/freee/reports                                │
│     ├─ 月次損益レポート                                       │
│     └─ PDF出力                                                │
└─────────────────────────────────────────────────────────────┘
```

実装場所: `lib/freee/`, `app/api/freee/`, `app/admin/accounting/`

### LINE連携

```
┌─────────────────────────────────────────────────────────────┐
│                    LINE連携フロー                              │
│                                                               │
│  1. Webhook受信                                               │
│     ├─ POST /api/integrations/line                           │
│     └─ メッセージ・イベント処理                               │
│                                                               │
│  2. 自動応答                                                   │
│     ├─ 物件情報の問い合わせ対応                               │
│     └─ リード登録                                             │
│                                                               │
│  3. プッシュ通知                                               │
│     ├─ 新着物件情報                                           │
│     └─ 打ち合わせリマインダー                                 │
└─────────────────────────────────────────────────────────────┘
```

実装場所: `app/api/integrations/line/`

### Cloudinary画像連携

```
┌─────────────────────────────────────────────────────────────┐
│                  Cloudinary画像処理フロー                      │
│                                                               │
│  1. 画像アップロード                                           │
│     ├─ POST /api/photos/presign                              │
│     └─ 署名付きURL発行                                        │
│                                                               │
│  2. 画像変換・最適化                                           │
│     ├─ リサイズ（サムネイル、中、大）                          │
│     ├─ WebP変換                                               │
│     └─ 透かし挿入                                             │
│                                                               │
│  3. CDN配信                                                   │
│     └─ 高速画像配信                                           │
└─────────────────────────────────────────────────────────────┘
```

実装場所: `app/api/photos/`, `app/api/upload-image/`

---

## 🏠 SUUMOクローリング

```
┌─────────────────────────────────────────────────────────────┐
│                   SUUMOクローリングフロー                      │
│                                                               │
│  1. SUUMO一覧ページ取得                                       │
│     └─ 対象URLから物件詳細URLを抽出                            │
│                                                               │
│  2. 物件詳細ページ取得                                        │
│     └─ タイトル/価格/住所/画像を抽出                           │
│                                                               │
│  3. 画像ロゴ合成                                              │
│     └─ 右下にC21ロゴを固定合成                                 │
│                                                               │
│  4. 公開用JSON生成                                           │
│     └─ public/suumo/properties.json                            │
└─────────────────────────────────────────────────────────────┘
```

実装場所: `scripts/crawl-suumo.mjs`  
出力先: `public/suumo/`  
定期実行: `.github/workflows/suumo-crawl.yml`（JST 月/水/金 00:01）

---

## 📐 開発ルール

### 1. ファイル配置ルール

| 作成するもの | 配置場所 | 理由 |
|------------|---------|------|
| ページコンポーネント | `app/admin/[module]/` | Next.js App Routerの規約 |
| API Route | `app/api/[module]/` | API エンドポイントの標準配置 |
| 再利用コンポーネント | `components/[category]/` | UIロジックの分離 |
| ビジネスロジック | `lib/[module]/` | **最重要**。ロジックを集約 |
| DB操作関数 | `lib/supabase/[module].ts` | データアクセス層の統一 |
| 型定義 | `types/[module].ts` | 型の一元管理 |
| Server Actions | `server/actions/[module].ts` | サーバーアクションの集約 |

### 2. コーディング規約

#### TypeScript

```typescript
// ✅ 良い例: 型定義を明示
interface Customer {
  id: string;
  name: string;
  email: string | null;
}

async function getCustomer(id: string): Promise<Customer> {
  // ...
}

// ❌ 悪い例: any型を使用
async function getCustomer(id): Promise<any> {
  // ...
}
```

#### ビジネスロジックの分離

```typescript
// ✅ 良い例: lib/にビジネスロジックを配置
// lib/supabase/customers.ts
export async function getCustomers(filters?: CustomerFilters) {
  const supabase = createServerClient();
  let query = supabase.from('customers').select('*');

  if (filters?.status) {
    query = query.eq('status', filters.status);
  }

  const { data, error } = await query;
  if (error) throw new Error(error.message);
  return data;
}

// app/api/customers/route.ts
import { getCustomers } from '@/lib/supabase/customers';

export async function GET(request: NextRequest) {
  const customers = await getCustomers();
  return NextResponse.json({ data: customers });
}

// ❌ 悪い例: API Route内に直接ロジックを書く
export async function GET(request: NextRequest) {
  const supabase = createServerClient();
  const { data } = await supabase.from('customers').select('*');
  return NextResponse.json({ data });
}
```

#### エラーハンドリング

```typescript
// ✅ 良い例: try-catchで例外をキャッチ
export async function GET(request: NextRequest) {
  try {
    const customers = await getCustomers();
    return NextResponse.json({ data: customers });
  } catch (error) {
    console.error('Error fetching customers:', error);
    return NextResponse.json(
      { error: 'Internal Server Error' },
      { status: 500 }
    );
  }
}
```

#### 権限チェック

```typescript
// ✅ 良い例: 必ず権限チェックを実装
export async function POST(request: NextRequest) {
  const user = await getAuth(request);
  if (!user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  const hasPermission = await checkPermission(user.id, 'customers', 'CREATE');
  if (!hasPermission) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 });
  }

  // ... ビジネスロジック
}
```

### 3. データベース操作ルール

#### マイグレーション

```sql
-- ✅ 良い例: マイグレーションファイルで管理
-- supabase/migrations/20260206000000_add_customer_source.sql
ALTER TABLE customers ADD COLUMN source VARCHAR(100);

-- ❌ 悪い例: 手動でDBを変更（追跡不可能）
```

#### RLS（Row Level Security）

```sql
-- ✅ 良い例: RLSポリシーを必ず設定
ALTER TABLE customers ENABLE ROW LEVEL SECURITY;

CREATE POLICY "customers_select_policy" ON customers
  FOR SELECT USING (auth.uid() = assigned_to);

-- ❌ 悪い例: RLSを無効化（セキュリティリスク）
ALTER TABLE customers DISABLE ROW LEVEL SECURITY;
```

### 4. 命名規約

| 対象 | 規約 | 例 |
|------|------|-----|
| ファイル名 | kebab-case | `customer-list.tsx` |
| コンポーネント | PascalCase | `CustomerList` |
| 関数 | camelCase | `getCustomers` |
| 定数 | UPPER_SNAKE_CASE | `MAX_UPLOAD_SIZE` |
| 型・インターフェース | PascalCase | `Customer`, `CustomerFilters` |
| データベーステーブル | snake_case | `customers`, `customer_leads` |
| API エンドポイント | kebab-case | `/api/customers`, `/api/customer-leads` |

### 5. Git コミットメッセージ

```
feat: 新機能追加
fix: バグ修正
refactor: リファクタリング
docs: ドキュメント更新
test: テスト追加
chore: ビルド・設定変更

例:
feat: 顧客検索機能を追加
fix: リード詳細ページのエラーを修正
refactor: 権限チェック関数を統一
```

---

## 🚀 新機能開発フロー

### ステップ1: 要件確認

1. **機能の目的を明確化**
   - この機能は何を解決するのか？
   - 誰が使うのか？（ロール: OWNER, ADMIN, STAFF, PART_TIME）
   - どの既存機能と関連するのか？

2. **このサイトマップで確認すること**
   - [ ] 類似機能が既に存在しないか？（主要機能マップを確認）
   - [ ] 使用する技術スタックは何か？（技術スタックを確認）
   - [ ] データベーステーブルは必要か？（データベース設計を確認）
   - [ ] API エンドポイントは必要か？（API設計を確認）
   - [ ] 権限設定は必要か？（認証・権限システムを確認）

### ステップ2: 設計

#### 2-1. データベース設計

```sql
-- 新しいテーブルが必要な場合
-- supabase/migrations/20260206000001_create_new_feature.sql

CREATE TABLE new_feature (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    name VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- RLS ポリシーの設定
ALTER TABLE new_feature ENABLE ROW LEVEL SECURITY;

CREATE POLICY "new_feature_select_policy" ON new_feature
  FOR SELECT USING (true);

-- トリガーの設定（更新日時の自動更新）
CREATE TRIGGER set_updated_at
BEFORE UPDATE ON new_feature
FOR EACH ROW
EXECUTE FUNCTION set_updated_at();
```

#### 2-2. 型定義

```typescript
// types/new-feature.ts
export interface NewFeature {
  id: string;
  name: string;
  created_at: string;
  updated_at: string;
}

export interface NewFeatureFilters {
  name?: string;
  // ...
}

export interface CreateNewFeatureInput {
  name: string;
  // ...
}
```

#### 2-3. 権限設定

```json
// config/permissions.json に追加
{
  "features": {
    "new_feature": {
      "name": "新機能",
      "description": "新機能の説明",
      "permissions": ["VIEW", "CREATE", "EDIT", "DELETE"]
    }
  },
  "roles": {
    "OWNER": {
      "permissions": ["new_feature.*"]
    },
    "ADMIN": {
      "permissions": ["new_feature.VIEW", "new_feature.CREATE"]
    }
  }
}
```

### ステップ3: 実装

#### 3-1. データアクセス層（lib/supabase/）

```typescript
// lib/supabase/new-feature.ts
import { createServerClient } from '@/lib/supabase/server';
import type { NewFeature, NewFeatureFilters, CreateNewFeatureInput } from '@/types/new-feature';

export async function getNewFeatures(filters?: NewFeatureFilters): Promise<NewFeature[]> {
  const supabase = createServerClient();
  let query = supabase.from('new_feature').select('*');

  if (filters?.name) {
    query = query.ilike('name', `%${filters.name}%`);
  }

  const { data, error } = await query;
  if (error) throw new Error(error.message);
  return data;
}

export async function createNewFeature(input: CreateNewFeatureInput): Promise<NewFeature> {
  const supabase = createServerClient();
  const { data, error } = await supabase
    .from('new_feature')
    .insert(input)
    .select()
    .single();

  if (error) throw new Error(error.message);
  return data;
}
```

#### 3-2. API Layer（app/api/）

```typescript
// app/api/new-feature/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { getAuth } from '@/lib/auth/session';
import { checkPermission } from '@/lib/auth/permissions';
import { getNewFeatures, createNewFeature } from '@/lib/supabase/new-feature';

export async function GET(request: NextRequest) {
  try {
    // 認証チェック
    const user = await getAuth(request);
    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    // 権限チェック
    const hasPermission = await checkPermission(user.id, 'new_feature', 'VIEW');
    if (!hasPermission) {
      return NextResponse.json({ error: 'Forbidden' }, { status: 403 });
    }

    // ビジネスロジック
    const data = await getNewFeatures();
    return NextResponse.json({ data });
  } catch (error) {
    console.error('Error in GET /api/new-feature:', error);
    return NextResponse.json({ error: 'Internal Server Error' }, { status: 500 });
  }
}

export async function POST(request: NextRequest) {
  try {
    const user = await getAuth(request);
    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    const hasPermission = await checkPermission(user.id, 'new_feature', 'CREATE');
    if (!hasPermission) {
      return NextResponse.json({ error: 'Forbidden' }, { status: 403 });
    }

    const body = await request.json();
    const data = await createNewFeature(body);
    return NextResponse.json({ data }, { status: 201 });
  } catch (error) {
    console.error('Error in POST /api/new-feature:', error);
    return NextResponse.json({ error: 'Internal Server Error' }, { status: 500 });
  }
}
```

#### 3-3. UI Layer（components/ + app/admin/）

```typescript
// components/new-feature/NewFeatureList.tsx
'use client';

import { useState, useEffect } from 'react';
import type { NewFeature } from '@/types/new-feature';

export function NewFeatureList() {
  const [features, setFeatures] = useState<NewFeature[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    async function fetchFeatures() {
      try {
        const response = await fetch('/api/new-feature');
        const { data } = await response.json();
        setFeatures(data);
      } catch (error) {
        console.error('Failed to fetch features:', error);
      } finally {
        setLoading(false);
      }
    }

    fetchFeatures();
  }, []);

  if (loading) return <div>Loading...</div>;

  return (
    <div>
      {features.map((feature) => (
        <div key={feature.id}>{feature.name}</div>
      ))}
    </div>
  );
}

// app/admin/new-feature/page.tsx
import { NewFeatureList } from '@/components/new-feature/NewFeatureList';

export default function NewFeaturePage() {
  return (
    <div>
      <h1>新機能</h1>
      <NewFeatureList />
    </div>
  );
}
```

### ステップ4: テスト

```typescript
// tests/new-feature.spec.ts
import { test, expect } from '@playwright/test';

test('新機能一覧ページが表示される', async ({ page }) => {
  await page.goto('/admin/new-feature');
  await expect(page.locator('h1')).toHaveText('新機能');
});

test('新機能を作成できる', async ({ page }) => {
  await page.goto('/admin/new-feature');
  await page.click('button:has-text("新規作成")');
  await page.fill('input[name="name"]', 'テスト機能');
  await page.click('button[type="submit"]');
  await expect(page.locator('text=テスト機能')).toBeVisible();
});
```

### ステップ5: デプロイ

1. **マイグレーション実行**
   ```bash
   npx supabase migration up
   ```

2. **ビルド確認**
   ```bash
   npm run build:ci
   ```

3. **本番デプロイ**
   ```bash
   git push origin main  # Vercel自動デプロイ
   ```

---

## 📊 チェックリスト

### 新機能開発前チェックリスト

- [ ] **要件確認**
  - [ ] 機能の目的が明確か？
  - [ ] 誰が使うか明確か？（ロール）
  - [ ] 類似機能が存在しないか確認した

- [ ] **設計**
  - [ ] データベーステーブル設計完了
  - [ ] RLSポリシー設計完了
  - [ ] 型定義作成完了
  - [ ] 権限設定完了
  - [ ] API設計完了

- [ ] **実装**
  - [ ] データアクセス層実装（lib/supabase/）
  - [ ] ビジネスロジック実装（lib/）
  - [ ] API実装（app/api/）
  - [ ] UI実装（components/ + app/admin/）
  - [ ] エラーハンドリング実装
  - [ ] 権限チェック実装

- [ ] **テスト**
  - [ ] E2Eテスト作成
  - [ ] 手動テスト実施
  - [ ] 権限テスト実施

- [ ] **ドキュメント**
  - [ ] このサイトマップ（SITEMAP.md）を更新
  - [ ] API仕様をコメントで記載

---

## 🔄 このサイトマップの更新方法

このサイトマップは**システムの設計図**です。
新しい機能を追加したら、必ずこのサイトマップを更新してください。

### 更新が必要なケース

1. **新しい主要機能を追加した**
   → 「主要機能マップ」セクションに追加

2. **新しいデータベーステーブルを作成した**
   → 「データベース設計」セクションに追加

3. **新しいAPIエンドポイントを作成した**
   → 「API設計」セクションに追加

4. **新しい権限機能を追加した**
   → 「認証・権限システム」セクションに追加

5. **新しい外部連携を追加した**
   → 「外部連携システム」セクションに追加

6. **技術スタックを変更した**
   → 「技術スタック」セクションを更新

### 更新例

```markdown
## 🎯 主要機能マップ

### コア業務機能（10モジュール）← 数字を更新

┌─────────────────────────────────────────────────────────────┐
│                      新機能名                                  │
│  実装場所: app/admin/new-feature/                             │
│  API: /api/new-feature/*                                      │
│  権限: new_feature.VIEW, new_feature.CREATE                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📞 サポート

### 開発で困ったら

1. **このサイトマップを再確認**
   - 既存の実装パターンを参考にする
   - 類似機能のコードを見る

2. **既存のコードを検索**
   ```bash
   # 機能名で検索
   grep -r "customers" app/ lib/ components/

   # ファイル名で検索
   find . -name "*customer*"
   ```

3. **エラーログを確認**
   - ブラウザコンソール
   - サーバーログ
   - Supabaseダッシュボード

---

**最終更新**: 2026-02-06
**メンテナー**: HomeMart開発チーム
